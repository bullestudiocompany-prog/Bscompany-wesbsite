import { supabase } from "./js/supabaseClient.js"; document.body.insertAdjacentHTML( "afterbegin", ` <div id="debug" style=" position:fixed; top:0; left:0; right:0; z-index:99999; background:#22c55e; color:#000; padding:15px; font-weight:bold; text-align:center; "> ✅ admin.js est bien chargé ! </div> ` ); console.log("ADMIN.JS CHARGÉ"); console.log("Supabase :", supabase); const loginForm = document.getElementById("loginForm"); const loginError = document.getElementById("loginError"); if (!loginForm) { alert("❌ ERREUR : loginForm introuvable dans admin.html"); } else { loginForm.addEventListener("submit", async (e) => { e.preventDefault(); loginError.textContent = "⏳ Test de connexion..."; loginError.style.color = "#3b82f6"; const email = document.getElementById("loginEmail").value.trim(); const password = document.getElementById("loginPassword").value; console.log("Email :", email); console.log("Tentative Supabase..."); try { const { data, error } = await supabase.auth.signInWithPassword({ email, password }); console.log("Réponse Supabase :", data, error); if (error) { loginError.style.color = "#ef4444"; loginError.textContent = "❌ " + error.message; return; } loginError.style.color = "#22c55e"; loginError.textContent = "✅ CONNEXION RÉUSSIE !"; alert("Connexion Supabase réussie !"); } catch (error) { console.error("ERREUR :", error); loginError.style.color = "#ef4444"; loginError.textContent = "❌ " + error.message; } }); } 
